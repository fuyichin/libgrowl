TODO
=====

(v) working
(x) done

Xcode
=====

Unix/Linux
=====

* automake file

libgrowl
=====

* ( ) [gntp_return_error] When server reply with error, shows what is the error.

* (x) What is the minimalist of GNTP header?

* Retrieve platform name and version.

* (v) [change_host] Can change ip and port

* Can use machine name as ip eg. "localhost" to replace '127.0.0.1"

* (x) function: char *gntp_get_header_value(
	return_value, 
	sizeof(return_value),
	header-code,
	gntp_message)
eg. header-code:Origin-Software-Name ==> libgrowl
The libgrowl store in return_value and also return in the function

* feature: return the fail reason from GNTP in debug level 1:
eg. Required header missing (Notification-Title)

* (x) rename libgrowl_set_debug() to libgrowl_set_debug_level()

growlnotify
=====

* print help message

VSCode
=====

* 'libgrowl.h' file not found, not configured properly (extension:C/C++ Clang Command Adapter aka clangd)
